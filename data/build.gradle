plugins { // TODO: Change to kotlin only
	id "com.android.library"
	id "kotlin-android"
}

dependencies {
	api project(":project:entity")

	implementation project(":data:database")

	implementation dep.kotlinx.coroutines.core

	implementation dep.koin.core
}